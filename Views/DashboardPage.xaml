<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="PFE.Views.DashboardPage"
             Title="Dashboard"
             BackgroundColor="#0F172A">

    <ScrollView>
        <Grid Padding="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!-- En-tête / Welcome -->
            <VerticalStackLayout Grid.Row="0"
                                 Spacing="6"
                                 Margin="0,10,0,20">
                <Label Text="Bienvenue 👋"
                       FontSize="28"
                       FontAttributes="Bold"
                       TextColor="White" />

                <Label Text="Vous êtes maintenant connecté à Odoo."
                       FontSize="14"
                       TextColor="#9CA3AF" />
            </VerticalStackLayout>

            <!-- Carte principale -->
            <VerticalStackLayout Grid.Row="1"
                                 Spacing="16">
                <Frame BackgroundColor="#111827"
                       CornerRadius="16"
                       HasShadow="True"
                       Padding="18"
                       BorderColor="#1F2937">
                    <VerticalStackLayout Spacing="16">

                        <Label Text="Mes actions"
                               FontSize="18"
                               FontAttributes="Bold"
                               TextColor="White" />

                        <!-- Boutons employés -->
                        <VerticalStackLayout Spacing="10">

                            <Button Text="Voir mes congés"
                                    x:Name="BtnLeaves"
                                    BackgroundColor="#22C55E"
                                    TextColor="White"
                                    CornerRadius="12"
                                    HeightRequest="48"
                                    FontSize="14"
                                    IsVisible="False" />

                            <Button Text="Rentrer une demande de congé"
                                    x:Name="BtnNewLeave"
                                    BackgroundColor="#3B82F6"
                                    TextColor="White"
                                    CornerRadius="12"
                                    HeightRequest="48"
                                    FontSize="14"
                                    IsVisible="False" />

                            <Button Text="Voir le calendrier"
                                    x:Name="BtnCalendar"
                                    BackgroundColor="#0EA5E9"
                                    TextColor="White"
                                    CornerRadius="12"
                                    HeightRequest="48"
                                    FontSize="14"
                                    IsVisible="False" />

                        </VerticalStackLayout>

                        <!-- Séparateur -->
                        <BoxView HeightRequest="1"
                                 BackgroundColor="#1F2937"
                                 Margin="0,8" />

                        <!-- Actions manager -->
                        <VerticalStackLayout Spacing="10">
                            <Label Text="Espace manager"
                                   FontSize="14"
                                   TextColor="#9CA3AF" />

                            <Button x:Name="BtnManageLeaves"
                                    Text="Gérer les demandes de congés"
                                    BackgroundColor="#F59E0B"
                                    TextColor="White"
                                    CornerRadius="12"
                                    HeightRequest="48"
                                    FontSize="14"
                                    IsVisible="False" />
                        </VerticalStackLayout>

                    </VerticalStackLayout>
                </Frame>

                <!-- Carte profil + déconnexion -->
                <Frame BackgroundColor="#111827"
                       CornerRadius="16"
                       HasShadow="True"
                       Padding="18"
                       BorderColor="#1F2937">
                    <VerticalStackLayout Spacing="12">
                        <Label Text="Mon compte"
                               FontSize="18"
                               FontAttributes="Bold"
                               TextColor="White" />

                        <Button Text="Voir mes informations"
                                x:Name="BtnProfile"
                                BackgroundColor="#4B5563"
                                TextColor="White"
                                CornerRadius="12"
                                HeightRequest="48"
                                FontSize="14" />

                        <Button Text="Se déconnecter"
                                x:Name="BtnLogout"
                                BackgroundColor="#DC2626"
                                TextColor="White"
                                CornerRadius="12"
                                HeightRequest="48"
                                FontSize="14" />
                    </VerticalStackLayout>
                </Frame>

            </VerticalStackLayout>
        </Grid>
    </ScrollView>
</ContentPage>
